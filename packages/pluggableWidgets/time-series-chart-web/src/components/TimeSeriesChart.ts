import { createElement, CSSProperties, ReactElement, ReactNode, useCallback, useRef, useState } from "react";
export interface Props {}

export const TimeSeriesChart = (props: Props): ReactElement => {
    return null;
};
