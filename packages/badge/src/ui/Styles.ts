import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row"
    },
    badge: {
        borderRadius: 30,
        paddingHorizontal: 10,
        paddingVertical: 5,
        backgroundColor: "#ccc"
    },
    text: {
        fontSize: 14,
        fontWeight: "bold",
        color: "#444",
        alignSelf: "center"
    }
});
