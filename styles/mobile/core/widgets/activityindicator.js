import { brand } from '../variables';

export const ActivityIndicator = {
    indicator: {
        // Just this property
        color: brand.primary,
    },
    container: {
        // All ViewStyle properties are allowed
    },
};
