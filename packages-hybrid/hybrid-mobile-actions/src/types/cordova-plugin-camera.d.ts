// Note: extend camera, missing typings
// eslint-disable-next-line no-unused-vars, @typescript-eslint/no-unused-vars
const enum CordovaCameraDestinationType {
    DATA_URL = 0,
    FILE_URI = 1,
    NATIVE_URI = 2
}

// eslint-disable-next-line no-unused-vars, @typescript-eslint/no-unused-vars
const enum CordovaCameraPictureSourceType {
    PHOTOLIBRARY = 0,
    CAMERA = 1,
    SAVEDPHOTOALBUM = 2
}
