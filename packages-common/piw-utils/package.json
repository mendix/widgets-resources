{
    "name": "@widgets-resources/piw-utils",
    "version": "1.0.0",
    "description": "Tools and helper functions for PIW",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "start": "tsc --watch",
        "prebuild": "rm -rf ./dist",
        "build": "tsc",
        "lint": "eslint --config ../../.eslintrc.js --ext .jsx,.js,.ts,.tsx src/",
        "prepare": "npm run build"
    }
}
