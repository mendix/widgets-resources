import { Component, ReactNode, createElement } from "react";
import { HelloWorldSample } from "./components/HelloWorldSample";
import { hot } from "react-hot-loader/root";
import { <%= widgetName %>ContainerProps } from "../typings/<%= widgetName %>Props";

import "./ui/<%= widgetName %>.css";

class <%= widgetName %> extends Component<<%= widgetName %>ContainerProps> {
    render(): ReactNode {
        return <HelloWorldSample sampleText={this.props.sampleText ? this.props.sampleText : "World"} />;
    }
}

export default hot(<%= widgetName %>);
