class HomePage {

    /**
     * Create your accessors to html elements here. Example:
     * badge() { return browser.element(".widget-badge.badge.mx-name-badge1"); }
     *
     * label() { return browser.element(".widget-badge.label.mx-name-badge7"); }
     */

    open() {
        browser.url("/");
    }
}
const homepage = new HomePage();
module.exports = homepage;
